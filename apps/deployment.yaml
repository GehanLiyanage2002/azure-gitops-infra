apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-app-deployment
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gitops-app
  template:
    metadata:
      labels:
        app: gitops-app
    spec:
      containers:
      - name: web-app
        image: acrigitopsprojectunique123.azurecr.io/gitops-app:24fb59891f9f2ed219c79a617c126b0326ded396 # Will be dynamically updated by CI
        ports:
        - containerPort: 3000